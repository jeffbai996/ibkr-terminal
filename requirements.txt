# ibkr_mcp dependencies
mcp>=1.0.0              # MCP SDK with FastMCP
ib_insync>=0.9.86       # IBKR API wrapper (or ib_async as drop-in replacement)
pydantic>=2.0.0         # Input validation (included with mcp, but pinning)
python-dotenv>=1.0.0    # Load .env files
nest_asyncio>=1.6.0     # Allows nested event loops (ib_insync inside FastMCP's loop)
uvicorn>=0.30.0,<0.35.0 # ASGI server for streamable-http transport (server_http.py)

# Dashboard (dashboard.py) — REST API served alongside MCP
yfinance>=0.2.40        # Yahoo Finance live prices (ibkr_quote has no market data sub)
httpx>=0.27.0           # Async HTTP client for Claude API passthrough (Query tab)

# Private core package — all tools and core infrastructure live here.
# Must be installed as editable from the sibling repo:
#   pip install -e ../ibkr-terminal-core
# DO NOT add core/ or tools/ directories to this repo — they shadow the package.

# Dev / testing
pytest>=8.0.0
